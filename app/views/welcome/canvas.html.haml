%h1 Geek Club
=fb_login_button
%ul.rules
  %li First rule: you don't talk about geek club.
  %li
    Second rule: you 
    %u don't
    talk about geek club.
-Shop.all.each do |i|
  =link_to "browse #{i.name}", "/proxy/#{BaseManager.uri_to_base64(i.uri)}"
